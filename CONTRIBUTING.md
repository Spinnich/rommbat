# Contributing to RomMBat

Thank you for considering contributing to RomMBat! This document outlines some guidelines to help you get started with your contributions.

**If you're looking to implement a large feature or make significant changes to the project, it's best to open an issue first AND join the [RomM Discord](https://discord.gg/invite/romm) to discuss your ideas with the maintainers.**

## Code of Conduct

Please note that this project adheres to the Contributor Covenant [code of conduct](CODE_OF_CONDUCT.md). By participating in this project, you are expected to uphold this code.

## AI Assistance Notice

> [!IMPORTANT]
>
> If you are using **any kind of AI assistance** to contribute to RomMBat, it must be disclosed in the pull request.

This norm comes from [RomM](https://github.com/rommapp/romm/blob/master/CONTRIBUTING.md) and RomMBat inherits it. It matters more here, not less, because **RomMBat itself is developed primarily with Claude Code**. Disclosure is what lets a reviewer calibrate how much scrutiny to apply, and a repository that is largely AI-authored has to be scrupulous about it or the signal is worthless.

Disclose the extent, not just the fact: docs only, code generation, or somewhere in between. If your PR responses are being generated by an AI, disclose that too. As a small exception, trivial tab-completion doesn't need to be disclosed.

An example disclosure:

> This PR was written primarily by Claude Code.

Or a more detailed disclosure:

> I consulted ChatGPT to understand the codebase but the solution
> was fully authored manually by myself.

Failure to disclose this is rude to the human operators on the other end of the pull request, but it also makes it difficult to determine how much scrutiny to apply to the contribution.

In a perfect world, AI assistance would produce equal or higher quality work than any human. That isn't the world we live in today, and in most cases it's generating slop.

Please be respectful to maintainers and disclose AI assistance.

### If you are an agent working in this repository

Read [CLAUDE.md](CLAUDE.md) first, then [docs/PLAN.md](docs/PLAN.md), which is the design of record. Load the skill in `.claude/skills/` that matches the task. Before claiming anything is done, run the `pre-pr-verification` skill and state plainly what you verified and what you did not.

## Contributing to the Docs

Documentation for RomMBat lives in this repository, under [docs/](docs/). Documentation for the RomM server itself lives in [the docs repo](https://github.com/rommapp/docs); open a pull request there for anything about RomM's own behaviour, including the device sync protocol.

## How to Contribute Code

1. Fork the repository.
2. Clone your forked repository: `git clone https://github.com/your-username/rommbat.git`
3. Checkout the `main` branch: `git checkout main`
4. Follow the steps in the [developer setup guide](DEVELOPER_SETUP.md)
5. Create a new branch for your feature/fix: `git checkout -b feature-or-fix-name`
6. Make your changes and commit them with descriptive commit messages: `git commit -am 'Add feature XYZ'`
7. Push your changes to your fork: `git push origin feature-or-fix-name`
8. Open a pull request to the `main` branch of the original repository.

## Pull Request Guidelines

- Make sure your code follows the project's coding standards.
- Test your changes locally before opening a pull request.
- Update the documentation if necessary.
- Ensure all existing tests pass, and add new tests for new functionality.
- Use clear and descriptive titles and descriptions for your pull requests.
- **Disclose AI assistance.** See above.

Before opening a PR:

```bash
dotnet build                    # no new warnings
dotnet test                     # full suite green
trunk fmt && trunk check        # never commit with --no-verify
cd reference && python3 verify.py
```

## Rules that are specific to this project

These are the ones a newcomer, human or otherwise, gets backwards. Each is cheap to follow and expensive to unwind.

1. **Never persist an absolute path.** RetroBat is portable and the drive letter changes. Store paths relative to the RetroBat root and resolve at the point of use. There is a static check for this; make sure it ran.
2. **Never edit an emulator INI.** `emulatorlauncher` regenerates emulator configs from EmulationStation options on every launch, so an INI edit is clobbered on the next boot. Write `es_settings.cfg` instead, which supports a per-game form: `<system>["<rom filename>"].<key>`.
3. **RetroBat is the authority on file extensions and required BIOS**, not RomM. Read `<extension>` from the live `es_systems.cfg`; join firmware against `batocera-systems.json` on md5 only.
4. **The EmulationStation hooks never touch the network.** They run inside the game-launch path. They append to a local journal and exit; a background pass flushes later.
5. **Nothing is written outside the RetroBat tree.** No `%APPDATA%`, no registry, no service, no scheduled task, no admin rights.
6. **Never commit secrets.** Tokens live in the local store. No token, no server URL, and no instance hostname belongs in the repo, in a config file, in a test fixture, or in an issue.

## Reference data

`reference/` vendors upstream files that the design depends on, so the numbers quoted in `docs/PLAN.md` can be re-derived offline and drift shows up in a diff.

```bash
cd reference && ./refresh.sh    # re-pull upstream, then re-derive every quoted number
```

**Never hand-edit a vendored file, and never "fix" `verify.py` by updating an expected number.** A drift there means an upstream fact moved, which is a signal to revisit `docs/PLAN.md`.

## Platform contributions

RomMBat supports platforms one certified system at a time, not in bulk. Certify per RetroBat system, never per aggregate: "RetroArch works" is not a claim anything can be verified against, because each libretro core has its own save naming, state directory and BIOS requirements.

If you are adding or fixing a platform, run the full checklist in the `platform-certification` skill and record the result in `docs/platforms/<system>.md`. A platform is not done at eight of nine. Never claim a platform works without having launched a game on it.

## Code Style

Follow the existing code style used throughout the project. `.editorconfig` and Trunk cover the mechanical parts; `trunk fmt` before you commit.

Beyond that:

- **English only** outside of localisation files.
- **No em-dashes** in comments, docs or commit messages. Use commas, parentheses, or separate sentences.
- **Keep comments short** and focused on why, not what. Don't narrate the code, and don't explain why a change was made; describe how the code behaves now.
- **Tests travel with code.** New logic gets a test. Save-shape and mapping logic get fixtures from a real install, checked in.

If working with VSCode, `.vscode/extensions.json` holds the recommended set and the editor offers to install it when you first open the workspace:

- [C#](https://marketplace.visualstudio.com/items?itemName=ms-dotnettools.csharp) for the language service, debugging and test discovery. [C# Dev Kit](https://marketplace.visualstudio.com/items?itemName=ms-dotnettools.csdevkit) adds a solution explorer on top, but its licence is free only for individuals and open source work, so the base extension is enough to contribute.
- [Trunk](https://marketplace.visualstudio.com/items?itemName=Trunk.io)
- [EditorConfig](https://marketplace.visualstudio.com/items?itemName=EditorConfig.EditorConfig)
- [Python](https://marketplace.visualstudio.com/items?itemName=ms-python.python) for `reference/verify.py`

Do not add the standalone Prettier or markdownlint extensions. Trunk runs both already at the versions pinned in `.trunk/trunk.yaml`, and a second copy formats to whatever it bundles instead, producing diffs that `trunk check` then rejects.

## Issue Reporting

If you encounter any bugs or have suggestions for improvements, please [create an issue](https://github.com/rommapp/rommbat/issues) on GitHub. Provide as much detail as possible, including steps to reproduce the issue if applicable.

Include your RomMBat, RetroBat and RomM versions, whether the install is portable, and the filesystem of the drive. Those three facts resolve a surprising share of reports on their own.

**Redact your RomM server URL and never paste an `rmm_` token into an issue.** For security issues, follow [SECURITY.md](SECURITY.md) instead of opening an issue.

## Licensing

By contributing to RomMBat, you agree that your contributions will be licensed under the project's [LICENSE](LICENSE) (GPL-3.0).

---

Thank you for contributing to RomMBat! Your help is greatly appreciated.
